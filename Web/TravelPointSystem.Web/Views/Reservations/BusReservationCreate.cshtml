@model TravelPointSystem.Web.ViewModels.Reservations.BusCreateInputModel
@{
    this.ViewData["Title"] = "New Bus reservation";
    var busesItems = Model.BusesItems.Select(x => new SelectListItem(x.Value, x.Key));
}

<div class="text-center mt-5 mb-3 h1">@this.ViewData["Title"]</div>

<form method="post" class="col-md-6 offset-md-3">
    <div asp-validation-summary="All" class="text-danger"></div>
    <div class="form-group">
        <label asp-for="BusNumber" class="control-label"></label>
        <select asp-for="BusNumber" class="form-control" asp-items="busesItems"></select>
        <span asp-validation-for="BusNumber" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-primary">Next</button>
</form>
